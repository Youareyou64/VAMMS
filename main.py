# Dependencies
import math

# Classes
import graph
import renderer

raw_function = input("Enter your function: f(x,y)=")


graph3D = graph(raw_function)
graph3D.fill(0,5,0,5)

#model = renderer
#renderer.__init__(model,graph3D.zArray)
#render(model)

